# ChangeBot

**ChangeBot** — это Telegram-бот, написанный на Python, предназначенный для автоматического обнаружения денежных сумм в сообщениях чата и их конвертации в другие валюты. Бот поддерживает работу как в группах, так и в инлайн-режиме.

Проект построен с использованием современного асинхронного стека технологий и готов к развертыванию через Docker.

## 📋 Содержание

- [Функциональность](#-функциональность)
- [Стек технологий](#-стек-технологий)
- [Структура проекта](#-структура-проекта)
- [Установка и запуск](#-установка-и-запуск)
  - [Предварительные требования](#предварительные-требования)
  - [Запуск через Docker (Рекомендуется)](#запуск-через-docker-рекомендуется)
  - [Локальный запуск (без Docker)](#локальный-запуск-без-docker)
- [Конфигурация](#-конфигурация)
- [Миграции базы данных](#-миграции-базы-данных)
- [Разработка](#-разработка)

## 🚀 Функциональность

*   **Автоматическая конвертация**: Бот "слушает" сообщения в чатах, находит суммы в одной валюте и предлагает перевод в целевые валюты.
*   **Инлайн-режим**: Возможность использовать бота в любом чате через `@botname` для быстрого расчета курсов.
*   **Актуальные курсы**: Использование внешних API (например, Open Exchange Rates) для получения свежих котировок.
*   **Администрирование**: Система управления через суперпользователей.
*   **Логирование**: Отправка логов в специальный чат или в консоль.

## 🛠 Стек технологий

*   **Язык**: Python 3.10+
*   **Фреймворк бота**: [aiogram 3.x](https://github.com/aiogram/aiogram) — современный асинхронный фреймворк для Telegram Bot API.
*   **База данных**: PostgreSQL (или SQLite для тестов).
*   **ORM**: SQLAlchemy (асинхронная версия).
*   **Миграции**: Alembic.
*   **Конфигурация**: Pydantic Settings.
*   **Контейнеризация**: Docker & Docker Compose.

## 📂 Структура проекта

```text
.
├── app/                 # (Legacy/Alternative) Модули конфигурации и моделей
├── config_dist/         # Примеры конфигурационных файлов
├── migrations/          # Файлы миграций базы данных (Alembic)
├── src/                 # Основной исходный код
│   ├── bot/             # Хендлеры, middleware и логика бота
│   ├── config.py        # Настройки приложения (Pydantic)
│   ├── database/        # Подключение к БД и модели
│   └── services/        # Бизнес-логика (конвертация, работа с API)
├── tests/               # Тесты
├── Dockerfile           # Инструкции для сборки образа
├── docker-compose.yml   # Оркестрация контейнеров
├── main.py              # Точка входа в приложение
├── alembic.ini          # Конфигурация Alembic
└── requirements.txt     # Зависимости проекта
```

## ⚙️ Установка и запуск

### Предварительные требования

*   Установленный [Docker](https://www.docker.com/) и Docker Compose.
*   Токен бота от [@BotFather](https://t.me/BotFather).
*   Токен API для курсов валют (например, Open Exchange Rates), если требуется.

### Запуск через Docker (Рекомендуется)

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Sin7009/ChangeBot.git
    cd ChangeBot
    ```

2.  **Настройте окружение:**
    Создайте файл `.env` или скопируйте пример конфигурации (если есть) и заполните необходимые переменные (Токен бота, доступы к БД).
    
    Пример переменных окружения (см. `src/config.py`):
    ```env
    BOT_TOKEN=ващ_токен_бота
    POSTGRES_USER=postgres
    POSTGRES_PASSWORD=password
    POSTGRES_DB=changebot
    POSTGRES_HOST=db
    OER_TOKEN=ваш_токен_open_exchange_rates
    ```

3.  **Запустите контейнеры:**
    ```bash
    docker-compose up -d --build
    ```
    Это поднимет контейнер с ботом и базу данных (если она прописана в `docker-compose.yml`).

### Локальный запуск (без Docker)

1.  **Создайте виртуальное окружение:**
    ```bash
    python3 -m venv venv
    source venv/bin/activate  # для Linux/macOS
    # venv\Scripts\activate     # для Windows
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Настройка БД:**
    Убедитесь, что у вас запущена PostgreSQL или другая поддерживаемая БД. Укажите данные для подключения в переменных окружения.

4.  **Примените миграции:**
    ```bash
    alembic upgrade head
    ```

5.  **Запустите бота:**
    ```bash
    python main.py
    ```

## 🔧 Конфигурация

Настройки проекта управляются через переменные окружения (Environment Variables), что позволяет легко менять конфигурацию в Docker.

Основные параметры (проверьте `src/config.py` для полного списка):

| Переменная | Описание |
|------------|----------|
| `BOT_TOKEN` | Токен Telegram бота |
| `DB_DSN` / `POSTGRES_...` | Строка подключения к базе данных |
| `LOG_LEVEL` | Уровень логирования (INFO, DEBUG) |
| `OER_TOKEN` | API ключ сервиса курсов валют |

В папке `config_dist/` могут находиться примеры конфигурационных файлов `config.yaml` или `logging.yaml`, если проект поддерживает гибридную конфигурацию.

## 🗄 Миграции базы данных

Для управления схемой базы данных используется **Alembic**.

*   **Создание новой миграции** (после изменения моделей):
    ```bash
    alembic revision --autogenerate -m "описание изменений"
    ```
*   **Применение миграций**:
    ```bash
    alembic upgrade head
    ```

## 💻 Разработка

Для разработчиков предусмотрен файл `dev.requirements.txt` с дополнительными инструментами (линтеры, форматтеры).

```bash
pip install -r dev.requirements.txt
```

Для запуска тестов:
```bash
pytest
```

---
*Разработано [Sin7009](https://github.com/Sin7009)*
